/*global define */

/**
 * This module handles character decisions when actions are made,
 * such as a sequence of walk and get down, when a pickup action is issued.
 */
define([
], function (
) {
    var decide = 2;

    return {
        'decide' : decide
    };
});